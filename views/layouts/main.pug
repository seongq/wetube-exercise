doctype html
html(lang="en")
    head
        title #{pageTitle} | #{siteName}
    body
        include ../partials/header
        main
            block content
        include ../partials/footer
        script(src="https://kit.fontawesome.com/690023aeeb.js" crossorigin="anonymous" )

        